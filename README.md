# Notas:
```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la tabla de multiplicar      [número] [requerido]
  -s, --show     Listar o no la tabla en consola     [booleano] [defecto: false]
  -l, --limit    Define el límite hasta donde llega la tabla
                                                          [número] [defecto: 10]
```